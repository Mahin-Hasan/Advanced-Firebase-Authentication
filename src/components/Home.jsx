// import { useContext } from "react";
// import { AuthContext } from "../providers/AuthProvider";

const Home = () => {
    // const authInformation = useContext(AuthContext)
    // console.log(authInformation);
    return (
        <div>
            {/* <h2>HOme for: {authInformation.name}</h2> */}
            <h2>HOme</h2>
        </div>
    );
};

export default Home;